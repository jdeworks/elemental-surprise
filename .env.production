# Used when running npm run build so docs/ gets CDN icon URLs (works on GitHub Pages).
# Override in CI if needed (e.g. different branch).
VITE_CDN_BASE=https://cdn.jsdelivr.net/gh/jdeworks/elemental-surprise@dev/public
